<!DOCTYPE html>

<html lang="en">

<head>
    <%- include('./partials/head'); %>
</head>

<body class="bg">
    <%- include('./partials/navbar') %>

        <main>
            <% if(images.length==0) { %>
                <p>Todavía no hay imágenes en tu galería. ¡Añade una haciendo click <a href="/new-image">aquí</a>!</p>
                <% } else{ %>
                    <p class="text-center m-4 fs-5 fw-medium text-danger-emphasis">Tu galería tiene <%= images.length %> imágenes</p>
                    <% } %>
                        <!-- Generar tants articles com imatges hi hagi -->
                        <section class="container py-4">
                            <div class="row justify-content-center g-4">
                                <% images.forEach(function (i) { %>
                                    <div class="col-12 col-sm-6 col-md-4 mb-4">
                                        <article
                                            class="card h-100 shadow-sm border border-light bg-red-300 rounded-5 text-center">
                                            <div class="card-body">
                                                <h5 class="card-title">
                                                    <%= i.title %>
                                                </h5>
                                                <img src="<%= i.url %>" alt="<%= i.title %>"
                                                    class="img-fluid rounded mb-2">
                                                <p class="card-text text-muted mb-1">
                                                    Fecha: <%= new Date(i.date).toLocaleDateString('es-ES', {
                                                        day: 'numeric' , month: 'long' , year: 'numeric' }) %>
                                                </p>
                                            </div>
                                        </article>
                                    </div>
                                    <% }) %>
                            </div>
                        </section>



        </main>
        <%- include('./partials/footer') %>

</body>

</html>